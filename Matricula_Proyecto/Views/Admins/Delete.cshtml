@model Matricula_Proyecto.Models.Admin

@{
    ViewBag.Title = "Eliminar Admin";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Admin</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.usuario.usuario_nombre)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.usuario.usuario_nombre)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.nombre_admin)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.nombre_admin)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
