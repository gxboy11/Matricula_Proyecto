@model IEnumerable<Matricula_Proyecto.Models.Matricula>

@{
    ViewBag.Title = "Matriculas";
}

<h2>Matriculas</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.estudiante.nombre_estudiante)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.horario.dia_semana)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.estudiante.nombre_estudiante)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.horario.dia_semana)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.matricula_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.matricula_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.matricula_id })
        </td>
    </tr>
}

</table>
